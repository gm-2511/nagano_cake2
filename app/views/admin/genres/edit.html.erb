<div class="container mt-4">
  <h3 class="mb-4">ジャンル編集</h3>

  <% if @genre.errors.any? %>
    <div class="alert alert-danger">
      <ul class="mb-0">
        <% @genre.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with model: [:admin, @genre], local: true do |f| %>
    <div style="display: flex; align-items: center; flex-wrap: wrap; gap: 10px;">

      <!-- ジャンル選択プルダウン -->
      <%= f.collection_select :id, @genres, :id, :name,
            { prompt: "選択してください" }, { class: "form-select", style: "width: 150px;" } %>

      <!-- 保存ボタン -->
      <%= f.submit "変更を保存", class: "btn btn-success" %>

      <!-- 戻るボタン -->
      <%= link_to "戻る", admin_genres_path, class: "btn btn-secondary" %>

    </div>
  <% end %>
</div>
